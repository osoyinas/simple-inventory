export function AsideSection({ children }: { children: React.ReactNode }) {

    return (
    <section className="p-8 w-full h-full min-w-min bg-base-200 text-base-content">
        {children}
    </section>
        )
}
